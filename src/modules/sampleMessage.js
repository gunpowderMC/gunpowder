module.exports = {
    dest: {
        type: 'all', // module, gunpowder, or all
        dest: 'threadName' // threadName can be a '*' to go to all of the selected type
    },
    msg: { // the payload

    }
}